diff -urN gzip-1.3.5/ChangeLog gzip-1.3.5-STEP/ChangeLog
--- gzip-1.3.5/ChangeLog	2002-09-30 21:20:09.000000000 +1200
+++ gzip-1.3.5-STEP/ChangeLog	2005-08-23 00:50:46.733644104 +1200
@@ -187,13 +187,13 @@
 
 2000-07-20  Paul Eggert  <eggert@twinsun.com>
 
-	Use a POSIX-compliant shell instead of /bin/sh.
+	Use a POSIX-compliant shell instead of /System/Tools/sh.
 	Idea suggested by Albert Chin-A-Young <china@thewrittenword.com>.
 
 	* m4/shell.m4: New file.
 	* configure.in (AC_PROG_SHELL): Add it.
 	* Makefile.am (ACINCLUDE_INPUTS): Add $(M4DIR)/shell.m4.
-	(.in): Replace /bin/sh with $(SHELL).
+	(.in): Replace /System/Tools/sh with $(SHELL).
 
 2000-04-17  Bruno Haible  <haible@clisp.cons.org>
 
@@ -587,7 +587,7 @@
 	 Added warning about 'rehash' in INSTALL.
 	 Removed default value of read_buf in bits.c (supermax doesn't like).
 	 In tailor.h, added support for Borland C and Zortech C on OS/2.
-	 Added warning in gzexe about Ultrix buggy sh (use /bin/sh5 instead).
+	 Added warning in gzexe about Ultrix buggy sh (use /System/Tools/sh5 instead).
 	 Added warning in zdiff about AIX buggy sh (use /bin/ksh instead).
 	 In configure.in, do not try the asm code if DEFS contains NO_ASM
 
@@ -716,7 +716,7 @@
 	   (Explicit request from Noah Friedman.)
 	  Add compile option GNU_STANDARD to respect the GNU coding standards:
 	    with -DGNU_STANDARD, behave as gzip even if invoked under the
-	    name gunzip. (Complaints to /dev/null or the FSF, not to me!)
+	    name gunzip. (Complaints to /System/Devices/null or the FSF, not to me!)
 
 Fri Mar 10 13:27:18 1993  Jean-loup Gailly  (jloup@chorus.fr)
 
@@ -750,10 +750,10 @@
 	   zforce accordingly.
 	  Allow a version number in input file names for VMS.
 	  Added sample program zread.c.
-	  Fix "make check" for some implementations of /bin/sh.
+	  Fix "make check" for some implementations of /System/Tools/sh.
 	  Don't rely on stat() for filenames with extension > 3 chars
 	    on MSDOS, OS2 and Atari.
-	  Garbage collect files in /tmp created by gzexe.
+	  Garbage collect files in /System/TemporaryFiles created by gzexe.
 	  Quote $opt in znew.
 	  Use TOUCH env variable in znew if it exists.
 	  Better error message for gunzip on empty or truncated file.
@@ -863,7 +863,7 @@
 
 	* version 1.0
 	  gzip now runs on Vax/VMS (Amiga support will come in next version).
-	  Do not overwrite files without -f when using /bin/sh.
+	  Do not overwrite files without -f when using /System/Tools/sh.
           Support the test option -t for compressed (.Z) files.
 	  Flush output for bad compressed files. Add warning in README.
 	  Added makefiles for MSDOS.
@@ -922,7 +922,7 @@
 	  Accept .exe as well as .EXE.
 	  Uncompress files with multiple links only with -f.
 	  Better error message for gunzip of non-existent file.z.
-	  Fix the entry for /etc/magic in INSTALL.
+	  Fix the entry for /System/Settings/magic in INSTALL.
 	  Use AC_HAVE_HEADERS uniformly instead of special macros.
 	  Install the man pages as .1 by default instead of .l.
 	  Document crypt++.el in README.
@@ -961,7 +961,7 @@
 	  Added "make test" to check for compiler bugs.
 	  Don't rely on `i386`; try to assemble directly
 	  Change magic header to avoid conflict with freeze 1.x.
-	  Added entry for /etc/magic in INSTALL.
+	  Added entry for /System/Settings/magic in INSTALL.
 	  Do not destroy an input .zip file with more than one member.
 	  Display "untested" instead of "OK" for gzip -t foo.Z
 	  With -t, skip stdin in .Z format
@@ -971,7 +971,7 @@
 
 Tue Dec 29 10:00:19 1992   Noah Friedman  (friedman@gnu.ai.mit.edu)
 
-	  Added test for #!/bin/sh in configure.in.
+	  Added test for #!/System/Tools/sh in configure.in.
 	  Fix some references to $srcdir in Makefile.in
 
 Mon Dec 21 17:33:35 1992  Jean-Loup Gailly  (jloup@chorus.fr)
diff -urN gzip-1.3.5/INSTALL gzip-1.3.5-STEP/INSTALL
--- gzip-1.3.5/INSTALL	2001-07-18 04:45:20.000000000 +1200
+++ gzip-1.3.5-STEP/INSTALL	2005-08-23 00:50:31.266995392 +1200
@@ -168,7 +168,7 @@
 you can create a site shell script called `config.site' that gives
 default values for variables like `CC', `cache_file', and `prefix'.
 `configure' looks for `PREFIX/share/config.site' if it exists, then
-`PREFIX/etc/config.site' if it exists.  Or, you can set the
+`PREFIX/System/Settings/config.site' if it exists.  Or, you can set the
 `CONFIG_SITE' environment variable to the location of the site script.
 A warning: not all `configure' scripts look for a site script.
 
@@ -203,7 +203,7 @@
 
 `--cache-file=FILE'
      Enable the cache: use and save the results of the tests in FILE,
-     traditionally `config.cache'.  FILE defaults to `/dev/null' to
+     traditionally `config.cache'.  FILE defaults to `/System/Devices/null' to
      disable caching.
 
 `--config-cache'
@@ -214,7 +214,7 @@
 `--silent'
 `-q'
      Do not print messages saying which checks are being made.  To
-     suppress all normal output, redirect it to `/dev/null' (any error
+     suppress all normal output, redirect it to `/System/Devices/null' (any error
      messages will still be shown).
 
 `--srcdir=DIR'
diff -urN gzip-1.3.5/Makefile.am gzip-1.3.5-STEP/Makefile.am
--- gzip-1.3.5/Makefile.am	2002-09-30 06:18:50.000000000 +1200
+++ gzip-1.3.5-STEP/Makefile.am	2005-08-23 00:50:39.804697464 +1200
@@ -70,7 +70,7 @@
 .in:
 	sed \
 		-e '$(SEDCMD)' \
-		-e 's|/bin/sh|$(SHELL)|g' \
+		-e 's|/System/Tools/sh|$(SHELL)|g' \
 		-e 's|BINDIR|$(bindir)|g' \
 		$(srcdir)/$@.in >$@
 	chmod a+x $@
diff -urN gzip-1.3.5/Makefile.in gzip-1.3.5-STEP/Makefile.in
--- gzip-1.3.5/Makefile.in	2002-09-30 21:28:48.000000000 +1200
+++ gzip-1.3.5-STEP/Makefile.in	2005-08-23 00:50:46.741642888 +1200
@@ -416,7 +416,7 @@
 uninstall-info-am:
 	$(PRE_UNINSTALL)
 	@if (install-info --version && \
-	     install-info --version | grep -i -v debian) >/dev/null 2>&1; then \
+	     install-info --version | grep -i -v debian) >/System/Devices/null 2>&1; then \
 	  list='$(INFO_DEPS)'; \
 	  for file in $$list; do \
 	    echo " install-info --info-dir=$(DESTDIR)$(infodir) --remove $(DESTDIR)$(infodir)/$$file"; \
@@ -631,7 +631,7 @@
 	mkdir $(distdir)/=inst
 	chmod a-w $(distdir)
 	dc_install_base=`$(am__cd) $(distdir)/=inst && pwd` \
-	  && dc_destdir="$${TMPDIR-/tmp}/am-dc-$$$$/" \
+	  && dc_destdir="$${TMPDIR-/System/TemporaryFiles}/am-dc-$$$$/" \
 	  && $(mkinstalldirs) $$dc_destdir \
 	  && cd $(distdir)/=build \
 	  && ../configure --srcdir=.. --prefix="$$dc_install_base" \
@@ -749,7 +749,7 @@
 	done
 	@$(POST_INSTALL)
 	@if (install-info --version && \
-	     install-info --version | grep -i -v debian) >/dev/null 2>&1; then \
+	     install-info --version | grep -i -v debian) >/System/Devices/null 2>&1; then \
 	  list='$(INFO_DEPS)'; \
 	  for file in $$list; do \
 	    echo " install-info --info-dir=$(DESTDIR)$(infodir) $(DESTDIR)$(infodir)/$$file";\
@@ -814,7 +814,7 @@
 .in:
 	sed \
 		-e '$(SEDCMD)' \
-		-e 's|/bin/sh|$(SHELL)|g' \
+		-e 's|/System/Tools/sh|$(SHELL)|g' \
 		-e 's|BINDIR|$(bindir)|g' \
 		$(srcdir)/$@.in >$@
 	chmod a+x $@
diff -urN gzip-1.3.5/NEWS gzip-1.3.5-STEP/NEWS
--- gzip-1.3.5/NEWS	2002-09-30 21:16:22.000000000 +1200
+++ gzip-1.3.5-STEP/NEWS	2005-08-23 00:50:46.749641672 +1200
@@ -32,7 +32,7 @@
 * Convert to Automake.
 * Fix bugs in the following areas:
   - files larger than 4 GB
-  - security hole involving symlinks from /tmp
+  - security hole involving symlinks from /System/TemporaryFiles
   - security hole involving long file names
   - permissions bug when compressing a symbolic link to a file
   - core dumps
@@ -162,7 +162,7 @@
   extract to .tar by default.
 * On some versions of MSDOS, files with a 3 character extension could not
   be compressed.
-* Garbage collect files in /tmp created by gzexe.
+* Garbage collect files in /System/TemporaryFiles created by gzexe.
 * Fix the 'OS code' byte in the gzip header.
 * For the Amiga, add the missing utime.h and add support for gcc.
 
@@ -211,7 +211,7 @@
 
 Major changes from 0.8.2 to 1.0
 * gzip now runs on Vax/VMS
-* gzip will not not overwrite files without -f when using /bin/sh in
+* gzip will not not overwrite files without -f when using /System/Tools/sh in
   background.
 * Support the test option -t for compressed (.Z) files.
   Allow some data recovery for bad .Z files.
@@ -233,7 +233,7 @@
 Major changes from 0.7 to 0.8:
 * gzip can now extract .z files created by 'pack'.
 * configure should no longer believe that every machine is a 386
-* Fix the entry for /etc/magic in INSTALL.
+* Fix the entry for /System/Settings/magic in INSTALL.
 * Add patch for GNU tar 1.11.1 and a pointer to crypt++.el
 * Uncompress files with multiple links only with -f.
 * Fix for uncompress of .Z files on 16-bit machines
diff -urN gzip-1.3.5/README gzip-1.3.5-STEP/README
--- gzip-1.3.5/README	2002-09-30 21:15:02.000000000 +1200
+++ gzip-1.3.5-STEP/README	2005-08-23 00:50:31.275994024 +1200
@@ -51,7 +51,7 @@
 See the file NEWS for a summary of changes since 0.5.  See the file
 INSTALL for installation instructions. Some answers to frequently
 asked questions are given in the file INSTALL, please read it. (In
-particular, please don't ask me once more for an /etc/magic entry.)
+particular, please don't ask me once more for an /System/Settings/magic entry.)
 
 WARNING: on several systems, compiler bugs cause gzip to fail, in
 particular when optimization options are on.  See the section "Special
diff -urN gzip-1.3.5/aclocal.m4 gzip-1.3.5-STEP/aclocal.m4
--- gzip-1.3.5/aclocal.m4	2002-09-30 21:28:35.000000000 +1200
+++ gzip-1.3.5-STEP/aclocal.m4	2005-08-23 00:50:39.826694120 +1200
@@ -71,7 +71,7 @@
 
 # test whether we have cygpath
 if test -z "$CYGPATH_W"; then
-  if (cygpath --version) >/dev/null 2>/dev/null; then
+  if (cygpath --version) >/System/Devices/null 2>/System/Devices/null; then
     CYGPATH_W='cygpath -w'
   else
     CYGPATH_W=echo
@@ -242,7 +242,7 @@
 # (eg FreeBSD returns the mod time of the symlink's containing
 # directory).
 if (
-   set X `ls -Lt $srcdir/configure conftest.file 2> /dev/null`
+   set X `ls -Lt $srcdir/configure conftest.file 2> /System/Devices/null`
    if test "$[*]" = "X"; then
       # -L didn't work.
       set X `ls -t $srcdir/configure conftest.file`
@@ -540,9 +540,9 @@
     if depmode=$depmode \
        source=conftest.c object=conftest.o \
        depfile=conftest.Po tmpdepfile=conftest.TPo \
-       $SHELL ./depcomp $depcc -c conftest.c -o conftest.o >/dev/null 2>&1 &&
-       grep conftest.h conftest.Po > /dev/null 2>&1 &&
-       ${MAKE-make} -s -f confmf > /dev/null 2>&1; then
+       $SHELL ./depcomp $depcc -c conftest.c -o conftest.o >/System/Devices/null 2>&1 &&
+       grep conftest.h conftest.Po > /System/Devices/null 2>&1 &&
+       ${MAKE-make} -s -f confmf > /System/Devices/null 2>&1; then
       am_cv_$1_dependencies_compiler_type=$depmode
       break
     fi
@@ -566,15 +566,15 @@
 # Choose a directory name for dependency files.
 # This macro is AC_REQUIREd in _AM_DEPENDENCIES
 AC_DEFUN([AM_SET_DEPDIR],
-[rm -f .deps 2>/dev/null
-mkdir .deps 2>/dev/null
+[rm -f .deps 2>/System/Devices/null
+mkdir .deps 2>/System/Devices/null
 if test -d .deps; then
   DEPDIR=.deps
 else
   # MS-DOS does not allow filenames that begin with a dot.
   DEPDIR=_deps
 fi
-rmdir .deps 2>/dev/null
+rmdir .deps 2>/System/Devices/null
 AC_SUBST([DEPDIR])
 ])
 
@@ -626,12 +626,12 @@
   # Grep'ing the first line is not enough: some people post-process
   # each Makefile.in and add a new line on top of each file to say so.
   # So let's grep whole file.
-  if grep '^#.*generated by automake' $mf > /dev/null 2>&1; then
+  if grep '^#.*generated by automake' $mf > /System/Devices/null 2>&1; then
     dirpart=`AS_DIRNAME("$mf")`
   else
     continue
   fi
-  grep '^DEP_FILES *= *[[^ @%:@]]' < "$mf" > /dev/null || continue
+  grep '^DEP_FILES *= *[[^ @%:@]]' < "$mf" > /System/Devices/null || continue
   # Extract the definition of DEP_FILES from the Makefile without
   # running `make'.
   DEPDIR=`sed -n -e '/^DEPDIR = / s///p' < "$mf"`
@@ -719,7 +719,7 @@
 # In particular we don't look at `^make:' because GNU make might
 # be invoked under some other name (usually "gmake"), in which
 # case it prints its new name instead of `make'.
-if test "`$am_make -s -f confmf 2> /dev/null | grep -v 'ing directory'`" = "done"; then
+if test "`$am_make -s -f confmf 2> /System/Devices/null | grep -v 'ing directory'`" = "done"; then
    am__include=include
    am__quote=
    _am_result=GNU
@@ -727,7 +727,7 @@
 # Now try BSD make style include.
 if test "$am__include" = "#"; then
    echo '.include "confinc"' > confmf
-   if test "`$am_make -s -f confmf 2> /dev/null`" = "done"; then
+   if test "`$am_make -s -f confmf 2> /System/Devices/null`" = "done"; then
       am__include=.include
       am__quote="\""
       _am_result=BSD
@@ -832,7 +832,7 @@
       case $SHELL in
       /*)
 	rm -f conftest.c
-	if ("$SHELL" -c "$ac_command") 2>/dev/null; then
+	if ("$SHELL" -c "$ac_command") 2>/System/Devices/null; then
 	  ac_cv_path_shell=$SHELL
 	fi;;
       esac
@@ -853,7 +853,7 @@
 	  /*)
 	    for ac_base in sh bash ksh sh5; do
 	      rm -f conftest.c
-	      if ("$as_dir/$ac_base" -c "$ac_command") 2>/dev/null; then
+	      if ("$as_dir/$ac_base" -c "$ac_command") 2>/System/Devices/null; then
 		ac_cv_path_shell=$as_dir/$ac_base
 		break
 	      fi
@@ -868,7 +868,7 @@
    AC_MSG_RESULT($ac_cv_path_shell)
    SHELL=$ac_cv_path_shell
    if test "$SHELL" = no; then
-     SHELL=/bin/sh
+     SHELL=/System/Tools/sh
      AC_MSG_WARN([using $SHELL, even though it does not conform to POSIX])
    fi
    AC_SUBST(SHELL)])
diff -urN gzip-1.3.5/configure gzip-1.3.5-STEP/configure
--- gzip-1.3.5/configure	2002-09-30 21:28:51.000000000 +1200
+++ gzip-1.3.5-STEP/configure	2005-08-23 01:11:34.115013312 +1200
@@ -1,4 +1,4 @@
-#! /bin/sh
+#! /System/Tools/sh
 # Guess values for system-dependent variables and create Makefiles.
 # Generated by GNU Autoconf 2.54 for gzip 1.3.5.
 #
@@ -13,18 +13,18 @@
 ## --------------------- ##
 
 # Be Bourne compatible
-if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then
+if test -n "${ZSH_VERSION+set}" && (emulate sh) >/System/Devices/null 2>&1; then
   emulate sh
   NULLCMD=:
   # Zsh 3.x and 4.x performs word splitting on ${1+"$@"}, which
   # is contrary to our usage.  Disable this feature.
   alias -g '${1+"$@"}'='"$@"'
-elif test -n "${BASH_VERSION+set}" && (set -o posix) >/dev/null 2>&1; then
+elif test -n "${BASH_VERSION+set}" && (set -o posix) >/System/Devices/null 2>&1; then
   set -o posix
 fi
 
 # Support unset when possible.
-if (FOO=FOO; unset FOO) >/dev/null 2>&1; then
+if (FOO=FOO; unset FOO) >/System/Devices/null 2>&1; then
   as_unset=unset
 else
   as_unset=false
@@ -48,13 +48,13 @@
 done
 
 # Required to use basename.
-if expr a : '\(a\)' >/dev/null 2>&1; then
+if expr a : '\(a\)' >/System/Devices/null 2>&1; then
   as_expr=expr
 else
   as_expr=false
 fi
 
-if (basename /) >/dev/null 2>&1 && test "X`basename / 2>&1`" = "X/"; then
+if (basename /) >/System/Devices/null 2>&1 && test "X`basename / 2>&1`" = "X/"; then
   as_basename=basename
 else
   as_basename=false
@@ -66,7 +66,7 @@
 $as_expr X/"$0" : '.*/\([^/][^/]*\)/*$' \| \
 	 X"$0" : 'X\(//\)$' \| \
 	 X"$0" : 'X\(/\)$' \| \
-	 .     : '\(.\)' 2>/dev/null ||
+	 .     : '\(.\)' 2>/System/Devices/null ||
 echo X/"$0" |
     sed '/^.*\/\([^/][^/]*\)\/*$/{ s//\1/; q; }
   	  /^X\/\(\/\/\)$/{ s//\1/; q; }
@@ -84,10 +84,10 @@
 
 # The user is always right.
 if test "${PATH_SEPARATOR+set}" != set; then
-  echo "#! /bin/sh" >conftest.sh
+  echo "#! /System/Tools/sh" >conftest.sh
   echo  "exit 0"   >>conftest.sh
   chmod +x conftest.sh
-  if (PATH="/nonexistent;."; conftest.sh) >/dev/null 2>&1; then
+  if (PATH="/nonexistent;."; conftest.sh) >/System/Devices/null 2>&1; then
     PATH_SEPARATOR=';'
   else
     PATH_SEPARATOR=:
@@ -98,7 +98,7 @@
 
   as_lineno_1=$LINENO
   as_lineno_2=$LINENO
-  as_lineno_3=`(expr $as_lineno_1 + 1) 2>/dev/null`
+  as_lineno_3=`(expr $as_lineno_1 + 1) 2>/System/Devices/null`
   test "x$as_lineno_1" != "x$as_lineno_2" &&
   test "x$as_lineno_3"  = "x$as_lineno_2"  || {
   # Find who we are.  Look in the path if we contain no path at all
@@ -127,7 +127,7 @@
   case $CONFIG_SHELL in
   '')
     as_save_IFS=$IFS; IFS=$PATH_SEPARATOR
-for as_dir in /bin$PATH_SEPARATOR/usr/bin$PATH_SEPARATOR$PATH
+for as_dir in /System/Tools$PATH_SEPARATOR/System/AdminTools$PATH_SEPARATOR$PATH
 do
   IFS=$as_save_IFS
   test -z "$as_dir" && as_dir=.
@@ -137,9 +137,9 @@
 	   if ("$as_dir/$as_base" -c '
   as_lineno_1=$LINENO
   as_lineno_2=$LINENO
-  as_lineno_3=`(expr $as_lineno_1 + 1) 2>/dev/null`
+  as_lineno_3=`(expr $as_lineno_1 + 1) 2>/System/Devices/null`
   test "x$as_lineno_1" != "x$as_lineno_2" &&
-  test "x$as_lineno_3"  = "x$as_lineno_2" ') 2>/dev/null; then
+  test "x$as_lineno_3"  = "x$as_lineno_2" ') 2>/System/Devices/null; then
 	     $as_unset BASH_ENV || test "${BASH_ENV+set}" != set || { BASH_ENV=; export BASH_ENV; }
 	     $as_unset ENV || test "${ENV+set}" != set || { ENV=; export ENV; }
 	     CONFIG_SHELL=$as_dir/$as_base
@@ -190,7 +190,7 @@
   *)       ECHO_N= ECHO_C='\c' ECHO_T= ;;
 esac
 
-if expr a : '\(a\)' >/dev/null 2>&1; then
+if expr a : '\(a\)' >/System/Devices/null 2>&1; then
   as_expr=expr
 else
   as_expr=false
@@ -198,7 +198,7 @@
 
 rm -f conf$$ conf$$.exe conf$$.file
 echo >conf$$.file
-if ln -s conf$$.file conf$$ 2>/dev/null; then
+if ln -s conf$$.file conf$$ 2>/System/Devices/null; then
   # We could just check for DJGPP; but this test a) works b) is more generic
   # and c) will remain valid once DJGPP supports symlinks (DJGPP 2.04).
   if test -f conf$$.exe; then
@@ -207,14 +207,14 @@
   else
     as_ln_s='ln -s'
   fi
-elif ln conf$$.file conf$$ 2>/dev/null; then
+elif ln conf$$.file conf$$ 2>/System/Devices/null; then
   as_ln_s=ln
 else
   as_ln_s='cp -p'
 fi
 rm -f conf$$ conf$$.exe conf$$.file
 
-if mkdir -p . 2>/dev/null; then
+if mkdir -p . 2>/System/Devices/null; then
   as_mkdir_p=:
 else
   as_mkdir_p=false
@@ -242,7 +242,7 @@
 # Name of the host.
 # hostname on some systems (SVR3.2, Linux) returns a bogus exit status,
 # so uname gets run too.
-ac_hostname=`(hostname || uname -n) 2>/dev/null | sed 1q`
+ac_hostname=`(hostname || uname -n) 2>/System/Devices/null | sed 1q`
 
 exec 6>&1
 
@@ -255,7 +255,7 @@
 subdirs=
 MFLAGS=
 MAKEFLAGS=
-SHELL=${CONFIG_SHELL-/bin/sh}
+SHELL=${CONFIG_SHELL-/System/Tools/sh}
 
 # Maximum number of lines to put in a shell here document.
 # This variable seems obsolete.  It should probably be removed, and
@@ -315,7 +315,7 @@
 ac_init_version=false
 # The variables have the same names as the options, with
 # dashes changed to underlines.
-cache_file=/dev/null
+cache_file=/System/Devices/null
 exec_prefix=NONE
 no_create=
 no_recursion=
@@ -393,7 +393,7 @@
   -disable-* | --disable-*)
     ac_feature=`expr "x$ac_option" : 'x-*disable-\(.*\)'`
     # Reject names that are not valid shell variable names.
-    expr "x$ac_feature" : ".*[^-_$as_cr_alnum]" >/dev/null &&
+    expr "x$ac_feature" : ".*[^-_$as_cr_alnum]" >/System/Devices/null &&
       { echo "$as_me: error: invalid feature name: $ac_feature" >&2
    { (exit 1); exit 1; }; }
     ac_feature=`echo $ac_feature | sed 's/-/_/g'`
@@ -402,7 +402,7 @@
   -enable-* | --enable-*)
     ac_feature=`expr "x$ac_option" : 'x-*enable-\([^=]*\)'`
     # Reject names that are not valid shell variable names.
-    expr "x$ac_feature" : ".*[^-_$as_cr_alnum]" >/dev/null &&
+    expr "x$ac_feature" : ".*[^-_$as_cr_alnum]" >/System/Devices/null &&
       { echo "$as_me: error: invalid feature name: $ac_feature" >&2
    { (exit 1); exit 1; }; }
     ac_feature=`echo $ac_feature | sed 's/-/_/g'`
@@ -584,7 +584,7 @@
   -with-* | --with-*)
     ac_package=`expr "x$ac_option" : 'x-*with-\([^=]*\)'`
     # Reject names that are not valid shell variable names.
-    expr "x$ac_package" : ".*[^-_$as_cr_alnum]" >/dev/null &&
+    expr "x$ac_package" : ".*[^-_$as_cr_alnum]" >/System/Devices/null &&
       { echo "$as_me: error: invalid package name: $ac_package" >&2
    { (exit 1); exit 1; }; }
     ac_package=`echo $ac_package| sed 's/-/_/g'`
@@ -597,7 +597,7 @@
   -without-* | --without-*)
     ac_package=`expr "x$ac_option" : 'x-*without-\(.*\)'`
     # Reject names that are not valid shell variable names.
-    expr "x$ac_package" : ".*[^-_$as_cr_alnum]" >/dev/null &&
+    expr "x$ac_package" : ".*[^-_$as_cr_alnum]" >/System/Devices/null &&
       { echo "$as_me: error: invalid package name: $ac_package" >&2
    { (exit 1); exit 1; }; }
     ac_package=`echo $ac_package | sed 's/-/_/g'`
@@ -629,7 +629,7 @@
   *=*)
     ac_envvar=`expr "x$ac_option" : 'x\([^=]*\)='`
     # Reject names that are not valid shell variable names.
-    expr "x$ac_envvar" : ".*[^_$as_cr_alnum]" >/dev/null &&
+    expr "x$ac_envvar" : ".*[^_$as_cr_alnum]" >/System/Devices/null &&
       { echo "$as_me: error: invalid variable name: $ac_envvar" >&2
    { (exit 1); exit 1; }; }
     ac_optarg=`echo "$ac_optarg" | sed "s/'/'\\\\\\\\''/g"`
@@ -639,7 +639,7 @@
   *)
     # FIXME: should be removed in autoconf 3.0.
     echo "$as_me: WARNING: you should use --build, --host, --target" >&2
-    expr "x$ac_option" : ".*[^-._$as_cr_alnum]" >/dev/null &&
+    expr "x$ac_option" : ".*[^-._$as_cr_alnum]" >/System/Devices/null &&
       echo "$as_me: WARNING: invalid host type: $ac_option" >&2
     : ${build_alias=$ac_option} ${host_alias=$ac_option} ${target_alias=$ac_option}
     ;;
@@ -697,19 +697,19 @@
 ac_tool_prefix=
 test -n "$host_alias" && ac_tool_prefix=$host_alias-
 
-test "$silent" = yes && exec 6>/dev/null
+test "$silent" = yes && exec 6>/System/Devices/null
 
 
 # Find the source files, if location was not specified.
 if test -z "$srcdir"; then
   ac_srcdir_defaulted=yes
   # Try the directory containing this script, then its parent.
-  ac_confdir=`(dirname "$0") 2>/dev/null ||
+  ac_confdir=`(dirname "$0") 2>/System/Devices/null ||
 $as_expr X"$0" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$0" : 'X\(//\)[^/]' \| \
          X"$0" : 'X\(//\)$' \| \
          X"$0" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$0" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -732,7 +732,7 @@
    { (exit 1); exit 1; }; }
   fi
 fi
-(cd $srcdir && test -r ./$ac_unique_file) 2>/dev/null ||
+(cd $srcdir && test -r ./$ac_unique_file) 2>/System/Devices/null ||
   { echo "$as_me: error: sources are in $srcdir, but \`cd $srcdir' does not work" >&2
    { (exit 1); exit 1; }; }
 srcdir=`echo "$srcdir" | sed 's%\([^\\/]\)[\\/]*$%\1%'`
@@ -951,22 +951,22 @@
 ## Platform. ##
 ## --------- ##
 
-hostname = `(hostname || uname -n) 2>/dev/null | sed 1q`
-uname -m = `(uname -m) 2>/dev/null || echo unknown`
-uname -r = `(uname -r) 2>/dev/null || echo unknown`
-uname -s = `(uname -s) 2>/dev/null || echo unknown`
-uname -v = `(uname -v) 2>/dev/null || echo unknown`
-
-/usr/bin/uname -p = `(/usr/bin/uname -p) 2>/dev/null || echo unknown`
-/bin/uname -X     = `(/bin/uname -X) 2>/dev/null     || echo unknown`
-
-/bin/arch              = `(/bin/arch) 2>/dev/null              || echo unknown`
-/usr/bin/arch -k       = `(/usr/bin/arch -k) 2>/dev/null       || echo unknown`
-/usr/convex/getsysinfo = `(/usr/convex/getsysinfo) 2>/dev/null || echo unknown`
-hostinfo               = `(hostinfo) 2>/dev/null               || echo unknown`
-/bin/machine           = `(/bin/machine) 2>/dev/null           || echo unknown`
-/usr/bin/oslevel       = `(/usr/bin/oslevel) 2>/dev/null       || echo unknown`
-/bin/universe          = `(/bin/universe) 2>/dev/null          || echo unknown`
+hostname = `(hostname || uname -n) 2>/System/Devices/null | sed 1q`
+uname -m = `(uname -m) 2>/System/Devices/null || echo unknown`
+uname -r = `(uname -r) 2>/System/Devices/null || echo unknown`
+uname -s = `(uname -s) 2>/System/Devices/null || echo unknown`
+uname -v = `(uname -v) 2>/System/Devices/null || echo unknown`
+
+/System/Tools/uname -p = `(/System/Tools/uname -p) 2>/System/Devices/null || echo unknown`
+/System/Tools/uname -X     = `(/System/Tools/uname -X) 2>/System/Devices/null     || echo unknown`
+
+/System/Tools/arch              = `(/System/Tools/arch) 2>/System/Devices/null              || echo unknown`
+/System/Tools/arch -k       = `(/System/Tools/arch -k) 2>/System/Devices/null       || echo unknown`
+/usr/convex/getsysinfo = `(/usr/convex/getsysinfo) 2>/System/Devices/null || echo unknown`
+hostinfo               = `(hostinfo) 2>/System/Devices/null               || echo unknown`
+/System/Tools/machine           = `(/System/Tools/machine) 2>/System/Devices/null           || echo unknown`
+/System/Tools/oslevel       = `(/System/Tools/oslevel) 2>/System/Devices/null       || echo unknown`
+/System/Tools/universe          = `(/System/Tools/universe) 2>/System/Devices/null          || echo unknown`
 
 _ASUNAME
 
@@ -1131,9 +1131,9 @@
 # Prefer explicitly selected file to automatically selected ones.
 if test -z "$CONFIG_SITE"; then
   if test "x$prefix" != xNONE; then
-    CONFIG_SITE="$prefix/share/config.site $prefix/etc/config.site"
+    CONFIG_SITE="$prefix/share/config.site $prefix/System/Settings/config.site"
   else
-    CONFIG_SITE="$ac_default_prefix/share/config.site $ac_default_prefix/etc/config.site"
+    CONFIG_SITE="$ac_default_prefix/share/config.site $ac_default_prefix/System/Settings/config.site"
   fi
 fi
 for ac_site_file in $CONFIG_SITE; do
@@ -1146,7 +1146,7 @@
 done
 
 if test -r "$cache_file"; then
-  # Some versions of bash will fail to source /dev/null (special
+  # Some versions of bash will fail to source /System/Devices/null (special
   # files actually), so we avoid doing that.
   if test -f "$cache_file"; then
     { echo "$as_me:$LINENO: loading cache $cache_file" >&5
@@ -1275,12 +1275,12 @@
 # Find a good install program.  We prefer a C program (faster),
 # so one script is as good as another.  But avoid the broken or
 # incompatible versions:
-# SysV /etc/install, /usr/sbin/install
-# SunOS /usr/etc/install
+# SysV /System/Settings/install, /usr/sbin/install
+# SunOS /usr/System/Settings/install
 # IRIX /sbin/install
 # AIX /bin/install
 # AmigaOS /C/install, which installs bootblocks on floppy discs
-# AIX 4 /usr/bin/installbsd, which doesn't work without a -g flag
+# AIX 4 /System/Tools/installbsd, which doesn't work without a -g flag
 # AFS /usr/afsws/bin/install, which mishandles nonexistent args
 # SVR4 /usr/ucb/install, which tries to use the nonexistent group "staff"
 # ./install, which can be erroneously created by make from ./install.sh.
@@ -1298,7 +1298,7 @@
   # Account for people who put trailing slashes in PATH elements.
 case $as_dir/ in
   ./ | .// | /cC/* | \
-  /etc/* | /usr/sbin/* | /usr/etc/* | /sbin/* | /usr/afsws/bin/* | \
+  /System/Settings/* | /System/AdminTools/* | /usr/afsws/bin/* | \
   /usr/ucb/* ) ;;
   *)
     # OSF1 and SCO ODT 3.0 have their own names for install.
@@ -1308,11 +1308,11 @@
       for ac_exec_ext in '' $ac_executable_extensions; do
         if $as_executable_p "$as_dir/$ac_prog$ac_exec_ext"; then
           if test $ac_prog = install &&
-            grep dspmsg "$as_dir/$ac_prog$ac_exec_ext" >/dev/null 2>&1; then
+            grep dspmsg "$as_dir/$ac_prog$ac_exec_ext" >/System/Devices/null 2>&1; then
             # AIX install.  It has an incompatible calling convention.
             :
           elif test $ac_prog = install &&
-            grep pwplus "$as_dir/$ac_prog$ac_exec_ext" >/dev/null 2>&1; then
+            grep pwplus "$as_dir/$ac_prog$ac_exec_ext" >/System/Devices/null 2>&1; then
             # program-specific install script used by HP pwplus--don't use.
             :
           else
@@ -1360,7 +1360,7 @@
 # (eg FreeBSD returns the mod time of the symlink's containing
 # directory).
 if (
-   set X `ls -Lt $srcdir/configure conftest.file 2> /dev/null`
+   set X `ls -Lt $srcdir/configure conftest.file 2> /System/Devices/null`
    if test "$*" = "X"; then
       # -L didn't work.
       set X `ls -t $srcdir/configure conftest.file`
@@ -1472,7 +1472,7 @@
 	@echo 'ac_maketemp="${MAKE}"'
 _ACEOF
 # GNU make sometimes prints "make[1]: Entering...", which would confuse us.
-eval `${MAKE-make} -f conftest.make 2>/dev/null | grep temp=`
+eval `${MAKE-make} -f conftest.make 2>/System/Devices/null | grep temp=`
 if test -n "$ac_maketemp"; then
   eval ac_cv_prog_make_${ac_make}_set=yes
 else
@@ -1500,7 +1500,7 @@
 
 # test whether we have cygpath
 if test -z "$CYGPATH_W"; then
-  if (cygpath --version) >/dev/null 2>/dev/null; then
+  if (cygpath --version) >/System/Devices/null 2>/System/Devices/null; then
     CYGPATH_W='cygpath -w'
   else
     CYGPATH_W=echo
@@ -1956,18 +1956,18 @@
 echo "$as_me:$LINENO:" \
      "checking for C compiler version" >&5
 ac_compiler=`set X $ac_compile; echo $2`
-{ (eval echo "$as_me:$LINENO: \"$ac_compiler --version </dev/null >&5\"") >&5
-  (eval $ac_compiler --version </dev/null >&5) 2>&5
+{ (eval echo "$as_me:$LINENO: \"$ac_compiler --version </System/Devices/null >&5\"") >&5
+  (eval $ac_compiler --version </System/Devices/null >&5) 2>&5
   ac_status=$?
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
   (exit $ac_status); }
-{ (eval echo "$as_me:$LINENO: \"$ac_compiler -v </dev/null >&5\"") >&5
-  (eval $ac_compiler -v </dev/null >&5) 2>&5
+{ (eval echo "$as_me:$LINENO: \"$ac_compiler -v </System/Devices/null >&5\"") >&5
+  (eval $ac_compiler -v </System/Devices/null >&5) 2>&5
   ac_status=$?
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
   (exit $ac_status); }
-{ (eval echo "$as_me:$LINENO: \"$ac_compiler -V </dev/null >&5\"") >&5
-  (eval $ac_compiler -V </dev/null >&5) 2>&5
+{ (eval echo "$as_me:$LINENO: \"$ac_compiler -V </System/Devices/null >&5\"") >&5
+  (eval $ac_compiler -V </System/Devices/null >&5) 2>&5
   ac_status=$?
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
   (exit $ac_status); }
@@ -2127,7 +2127,7 @@
   ac_status=$?
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
   (exit $ac_status); }; then
-  for ac_file in `(ls conftest.o conftest.obj; ls conftest.*) 2>/dev/null`; do
+  for ac_file in `(ls conftest.o conftest.obj; ls conftest.*) 2>/System/Devices/null`; do
   case $ac_file in
     *.$ac_ext | *.xcoff | *.tds | *.d | *.pdb | *.xSYM | *.bb | *.bbg ) ;;
     *) ac_cv_objext=`expr "$ac_file" : '.*\.\(.*\)'`
@@ -2451,15 +2451,15 @@
 ac_compile='$CC -c $CFLAGS $CPPFLAGS conftest.$ac_ext >&5'
 ac_link='$CC -o conftest$ac_exeext $CFLAGS $CPPFLAGS $LDFLAGS conftest.$ac_ext $LIBS >&5'
 ac_compiler_gnu=$ac_cv_c_compiler_gnu
-rm -f .deps 2>/dev/null
-mkdir .deps 2>/dev/null
+rm -f .deps 2>/System/Devices/null
+mkdir .deps 2>/System/Devices/null
 if test -d .deps; then
   DEPDIR=.deps
 else
   # MS-DOS does not allow filenames that begin with a dot.
   DEPDIR=_deps
 fi
-rmdir .deps 2>/dev/null
+rmdir .deps 2>/System/Devices/null
 
 
           ac_config_commands="$ac_config_commands depfiles"
@@ -2483,7 +2483,7 @@
 # In particular we don't look at `^make:' because GNU make might
 # be invoked under some other name (usually "gmake"), in which
 # case it prints its new name instead of `make'.
-if test "`$am_make -s -f confmf 2> /dev/null | grep -v 'ing directory'`" = "done"; then
+if test "`$am_make -s -f confmf 2> /System/Devices/null | grep -v 'ing directory'`" = "done"; then
    am__include=include
    am__quote=
    _am_result=GNU
@@ -2491,7 +2491,7 @@
 # Now try BSD make style include.
 if test "$am__include" = "#"; then
    echo '.include "confinc"' > confmf
-   if test "`$am_make -s -f confmf 2> /dev/null`" = "done"; then
+   if test "`$am_make -s -f confmf 2> /System/Devices/null`" = "done"; then
       am__include=.include
       am__quote="\""
       _am_result=BSD
@@ -2574,9 +2574,9 @@
     if depmode=$depmode \
        source=conftest.c object=conftest.o \
        depfile=conftest.Po tmpdepfile=conftest.TPo \
-       $SHELL ./depcomp $depcc -c conftest.c -o conftest.o >/dev/null 2>&1 &&
-       grep conftest.h conftest.Po > /dev/null 2>&1 &&
-       ${MAKE-make} -s -f confmf > /dev/null 2>&1; then
+       $SHELL ./depcomp $depcc -c conftest.c -o conftest.o >/System/Devices/null 2>&1 &&
+       grep conftest.h conftest.Po > /System/Devices/null 2>&1 &&
+       ${MAKE-make} -s -f confmf > /System/Devices/null 2>&1; then
       am_cv_CC_dependencies_compiler_type=$depmode
       break
     fi
@@ -2644,7 +2644,7 @@
   rm -f conftest.er1
   cat conftest.err >&5
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); } >/dev/null; then
+  (exit $ac_status); } >/System/Devices/null; then
   if test -s conftest.err; then
     ac_cpp_err=$ac_c_preproc_warn_flag
   else
@@ -2677,7 +2677,7 @@
   rm -f conftest.er1
   cat conftest.err >&5
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); } >/dev/null; then
+  (exit $ac_status); } >/System/Devices/null; then
   if test -s conftest.err; then
     ac_cpp_err=$ac_c_preproc_warn_flag
   else
@@ -2735,7 +2735,7 @@
   rm -f conftest.er1
   cat conftest.err >&5
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); } >/dev/null; then
+  (exit $ac_status); } >/System/Devices/null; then
   if test -s conftest.err; then
     ac_cpp_err=$ac_c_preproc_warn_flag
   else
@@ -2768,7 +2768,7 @@
   rm -f conftest.er1
   cat conftest.err >&5
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); } >/dev/null; then
+  (exit $ac_status); } >/System/Devices/null; then
   if test -s conftest.err; then
     ac_cpp_err=$ac_c_preproc_warn_flag
   else
@@ -2835,7 +2835,7 @@
       case $SHELL in
       /*)
 	rm -f conftest.c
-	if ("$SHELL" -c "$ac_command") 2>/dev/null; then
+	if ("$SHELL" -c "$ac_command") 2>/System/Devices/null; then
 	  ac_cv_path_shell=$SHELL
 	fi;;
       esac
@@ -2856,7 +2856,7 @@
 	  /*)
 	    for ac_base in sh bash ksh sh5; do
 	      rm -f conftest.c
-	      if ("$as_dir/$ac_base" -c "$ac_command") 2>/dev/null; then
+	      if ("$as_dir/$ac_base" -c "$ac_command") 2>/System/Devices/null; then
 		ac_cv_path_shell=$as_dir/$ac_base
 		break
 	      fi
@@ -2874,7 +2874,7 @@
 echo "${ECHO_T}$ac_cv_path_shell" >&6
    SHELL=$ac_cv_path_shell
    if test "$SHELL" = no; then
-     SHELL=/bin/sh
+     SHELL=/System/Tools/sh
      { echo "$as_me:$LINENO: WARNING: using $SHELL, even though it does not conform to POSIX" >&5
 echo "$as_me: WARNING: using $SHELL, even though it does not conform to POSIX" >&2;}
    fi
@@ -2886,7 +2886,7 @@
 if test "${ac_cv_prog_egrep+set}" = set; then
   echo $ECHO_N "(cached) $ECHO_C" >&6
 else
-  if echo a | (grep -E '(a|b)') >/dev/null 2>&1
+  if echo a | (grep -E '(a|b)') >/System/Devices/null 2>&1
     then ac_cv_prog_egrep='grep -E'
     else ac_cv_prog_egrep='egrep'
     fi
@@ -2908,7 +2908,7 @@
 
 _ACEOF
 if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
-  $EGREP "yes" >/dev/null 2>&1; then
+  $EGREP "yes" >/System/Devices/null 2>&1; then
   echo "$as_me:$LINENO: result: yes" >&5
 echo "${ECHO_T}yes" >&6
 cat >>confdefs.h <<\_ACEOF
@@ -2949,7 +2949,7 @@
   rm -f conftest.er1
   cat conftest.err >&5
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); } >/dev/null; then
+  (exit $ac_status); } >/System/Devices/null; then
   if test -s conftest.err; then
     ac_cpp_err=$ac_c_preproc_warn_flag
   else
@@ -2976,7 +2976,7 @@
 
 _ACEOF
 if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
-  $EGREP "memchr" >/dev/null 2>&1; then
+  $EGREP "memchr" >/System/Devices/null 2>&1; then
   :
 else
   ac_cv_header_stdc=no
@@ -2994,7 +2994,7 @@
 
 _ACEOF
 if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
-  $EGREP "free" >/dev/null 2>&1; then
+  $EGREP "free" >/System/Devices/null 2>&1; then
   :
 else
   ac_cv_header_stdc=no
@@ -3180,7 +3180,7 @@
   rm -f conftest.er1
   cat conftest.err >&5
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); } >/dev/null; then
+  (exit $ac_status); } >/System/Devices/null; then
   if test -s conftest.err; then
     ac_cpp_err=$ac_c_preproc_warn_flag
   else
@@ -3569,7 +3569,7 @@
   ac_status=$?
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
   (exit $ac_status); }; }; then
-  nm conftest.$OBJEXT | grep _foo >/dev/null 2>&1 ||
+  nm conftest.$OBJEXT | grep _foo >/System/Devices/null 2>&1 ||
 	gzip_cv_underline=no
 else
   echo "$as_me: failed program was:" >&5
@@ -3596,10 +3596,10 @@
    *' NO_ASM '*) ;;
    *)
      if cp $srcdir/match.c _match.S &&
-        eval "$ASCPP _match.S > _match.s 2>/dev/null"; then
-       if test ! -s _match.s || grep error < _match.s > /dev/null; then
+        eval "$ASCPP _match.S > _match.s 2>/System/Devices/null"; then
+       if test ! -s _match.s || grep error < _match.s > /System/Devices/null; then
 	 :
-       elif eval "$CC -c _match.s >/dev/null 2>&1" &&
+       elif eval "$CC -c _match.s >/System/Devices/null 2>&1" &&
 	    test -f _match.$OBJEXT; then
 	 gzip_cv_assembler=yes
        fi
@@ -3788,7 +3788,7 @@
   rm -f conftest.er1
   cat conftest.err >&5
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); } >/dev/null; then
+  (exit $ac_status); } >/System/Devices/null; then
   if test -s conftest.err; then
     ac_cpp_err=$ac_c_preproc_warn_flag
   else
@@ -3815,7 +3815,7 @@
 
 _ACEOF
 if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
-  $EGREP "memchr" >/dev/null 2>&1; then
+  $EGREP "memchr" >/System/Devices/null 2>&1; then
   :
 else
   ac_cv_header_stdc=no
@@ -3833,7 +3833,7 @@
 
 _ACEOF
 if (eval "$ac_cpp conftest.$ac_ext") 2>&5 |
-  $EGREP "free" >/dev/null 2>&1; then
+  $EGREP "free" >/System/Devices/null 2>&1; then
   :
 else
   ac_cv_header_stdc=no
@@ -3971,7 +3971,7 @@
   rm -f conftest.er1
   cat conftest.err >&5
   echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); } >/dev/null; then
+  (exit $ac_status); } >/System/Devices/null; then
   if test -s conftest.err; then
     ac_cpp_err=$ac_c_preproc_warn_flag
   else
@@ -4590,11 +4590,11 @@
 if test "${ac_cv_sys_interpreter+set}" = set; then
   echo $ECHO_N "(cached) $ECHO_C" >&6
 else
-  echo '#! /bin/cat
+  echo '#! /System/Tools/cat
 exit 69
 ' >conftest
 chmod u+x conftest
-(SHELL=/bin/sh; export SHELL; ./conftest >/dev/null)
+(SHELL=/System/Tools/sh; export SHELL; ./conftest >/System/Devices/null)
 if test $? -ne 69; then
    ac_cv_sys_interpreter=yes
 else
@@ -4653,24 +4653,24 @@
 fi
 
   if test -n $ac_prefix_program; then
-    prefix=`(dirname "$ac_prefix_program") 2>/dev/null ||
+    prefix=`(dirname "$ac_prefix_program") 2>/System/Devices/null ||
 $as_expr X"$ac_prefix_program" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$ac_prefix_program" : 'X\(//\)[^/]' \| \
          X"$ac_prefix_program" : 'X\(//\)$' \| \
          X"$ac_prefix_program" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$ac_prefix_program" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
   	  /^X\(\/\/\)$/{ s//\1/; q; }
   	  /^X\(\/\).*/{ s//\1/; q; }
   	  s/.*/./; q'`
-    prefix=`(dirname "$prefix") 2>/dev/null ||
+    prefix=`(dirname "$prefix") 2>/System/Devices/null ||
 $as_expr X"$prefix" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$prefix" : 'X\(//\)[^/]' \| \
          X"$prefix" : 'X\(//\)$' \| \
          X"$prefix" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$prefix" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -4729,7 +4729,7 @@
      : end' >>confcache
 if cmp -s $cache_file confcache; then :; else
   if test -w $cache_file; then
-    test "x$cache_file" != "x/dev/null" && echo "updating cache $cache_file"
+    test "x$cache_file" != "x/System/Devices/null" && echo "updating cache $cache_file"
     cat confcache >$cache_file
   else
     echo "not updating unwritable cache $cache_file"
@@ -4810,18 +4810,18 @@
 ## --------------------- ##
 
 # Be Bourne compatible
-if test -n "${ZSH_VERSION+set}" && (emulate sh) >/dev/null 2>&1; then
+if test -n "${ZSH_VERSION+set}" && (emulate sh) >/System/Devices/null 2>&1; then
   emulate sh
   NULLCMD=:
   # Zsh 3.x and 4.x performs word splitting on ${1+"$@"}, which
   # is contrary to our usage.  Disable this feature.
   alias -g '${1+"$@"}'='"$@"'
-elif test -n "${BASH_VERSION+set}" && (set -o posix) >/dev/null 2>&1; then
+elif test -n "${BASH_VERSION+set}" && (set -o posix) >/System/Devices/null 2>&1; then
   set -o posix
 fi
 
 # Support unset when possible.
-if (FOO=FOO; unset FOO) >/dev/null 2>&1; then
+if (FOO=FOO; unset FOO) >/System/Devices/null 2>&1; then
   as_unset=unset
 else
   as_unset=false
@@ -4845,13 +4845,13 @@
 done
 
 # Required to use basename.
-if expr a : '\(a\)' >/dev/null 2>&1; then
+if expr a : '\(a\)' >/System/Devices/null 2>&1; then
   as_expr=expr
 else
   as_expr=false
 fi
 
-if (basename /) >/dev/null 2>&1 && test "X`basename / 2>&1`" = "X/"; then
+if (basename /) >/System/Devices/null 2>&1 && test "X`basename / 2>&1`" = "X/"; then
   as_basename=basename
 else
   as_basename=false
@@ -4863,7 +4863,7 @@
 $as_expr X/"$0" : '.*/\([^/][^/]*\)/*$' \| \
 	 X"$0" : 'X\(//\)$' \| \
 	 X"$0" : 'X\(/\)$' \| \
-	 .     : '\(.\)' 2>/dev/null ||
+	 .     : '\(.\)' 2>/System/Devices/null ||
 echo X/"$0" |
     sed '/^.*\/\([^/][^/]*\)\/*$/{ s//\1/; q; }
   	  /^X\/\(\/\/\)$/{ s//\1/; q; }
@@ -4881,10 +4881,10 @@
 
 # The user is always right.
 if test "${PATH_SEPARATOR+set}" != set; then
-  echo "#! /bin/sh" >conftest.sh
+  echo "#! /System/Tools/sh" >conftest.sh
   echo  "exit 0"   >>conftest.sh
   chmod +x conftest.sh
-  if (PATH="/nonexistent;."; conftest.sh) >/dev/null 2>&1; then
+  if (PATH="/nonexistent;."; conftest.sh) >/System/Devices/null 2>&1; then
     PATH_SEPARATOR=';'
   else
     PATH_SEPARATOR=:
@@ -4895,7 +4895,7 @@
 
   as_lineno_1=$LINENO
   as_lineno_2=$LINENO
-  as_lineno_3=`(expr $as_lineno_1 + 1) 2>/dev/null`
+  as_lineno_3=`(expr $as_lineno_1 + 1) 2>/System/Devices/null`
   test "x$as_lineno_1" != "x$as_lineno_2" &&
   test "x$as_lineno_3"  = "x$as_lineno_2"  || {
   # Find who we are.  Look in the path if we contain no path at all
@@ -4935,9 +4935,9 @@
 	   if ("$as_dir/$as_base" -c '
   as_lineno_1=$LINENO
   as_lineno_2=$LINENO
-  as_lineno_3=`(expr $as_lineno_1 + 1) 2>/dev/null`
+  as_lineno_3=`(expr $as_lineno_1 + 1) 2>/System/Devices/null`
   test "x$as_lineno_1" != "x$as_lineno_2" &&
-  test "x$as_lineno_3"  = "x$as_lineno_2" ') 2>/dev/null; then
+  test "x$as_lineno_3"  = "x$as_lineno_2" ') 2>/System/Devices/null; then
 	     $as_unset BASH_ENV || test "${BASH_ENV+set}" != set || { BASH_ENV=; export BASH_ENV; }
 	     $as_unset ENV || test "${ENV+set}" != set || { ENV=; export ENV; }
 	     CONFIG_SHELL=$as_dir/$as_base
@@ -4989,7 +4989,7 @@
   *)       ECHO_N= ECHO_C='\c' ECHO_T= ;;
 esac
 
-if expr a : '\(a\)' >/dev/null 2>&1; then
+if expr a : '\(a\)' >/System/Devices/null 2>&1; then
   as_expr=expr
 else
   as_expr=false
@@ -4997,7 +4997,7 @@
 
 rm -f conf$$ conf$$.exe conf$$.file
 echo >conf$$.file
-if ln -s conf$$.file conf$$ 2>/dev/null; then
+if ln -s conf$$.file conf$$ 2>/System/Devices/null; then
   # We could just check for DJGPP; but this test a) works b) is more generic
   # and c) will remain valid once DJGPP supports symlinks (DJGPP 2.04).
   if test -f conf$$.exe; then
@@ -5006,14 +5006,14 @@
   else
     as_ln_s='ln -s'
   fi
-elif ln conf$$.file conf$$ 2>/dev/null; then
+elif ln conf$$.file conf$$ 2>/System/Devices/null; then
   as_ln_s=ln
 else
   as_ln_s='cp -p'
 fi
 rm -f conf$$ conf$$.exe conf$$.file
 
-if mkdir -p . 2>/dev/null; then
+if mkdir -p . 2>/System/Devices/null; then
   as_mkdir_p=:
 else
   as_mkdir_p=false
@@ -5061,7 +5061,7 @@
   $ $0 $@
 
 _CSEOF
-echo "on `(hostname || uname -n) 2>/dev/null | sed 1q`" >&5
+echo "on `(hostname || uname -n) 2>/System/Devices/null | sed 1q`" >&5
 echo >&5
 _ACEOF
 
@@ -5237,9 +5237,9 @@
 }
 
 # Create a (secure) tmp directory for tmp files.
-: ${TMPDIR=/tmp}
+: ${TMPDIR=/System/TemporaryFiles}
 {
-  tmp=`(umask 077 && mktemp -d -q "$TMPDIR/csXXXXXX") 2>/dev/null` &&
+  tmp=`(umask 077 && mktemp -d -q "$TMPDIR/csXXXXXX") 2>/System/Devices/null` &&
   test -n "$tmp" && test -d "$tmp"
 }  ||
 {
@@ -5395,12 +5395,12 @@
   esac
 
   # Compute @srcdir@, @top_srcdir@, and @INSTALL@ for subdirectories.
-  ac_dir=`(dirname "$ac_file") 2>/dev/null ||
+  ac_dir=`(dirname "$ac_file") 2>/System/Devices/null ||
 $as_expr X"$ac_file" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$ac_file" : 'X\(//\)[^/]' \| \
          X"$ac_file" : 'X\(//\)$' \| \
          X"$ac_file" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$ac_file" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5414,12 +5414,12 @@
     as_dirs=
     while test ! -d "$as_dir"; do
       as_dirs="$as_dir $as_dirs"
-      as_dir=`(dirname "$as_dir") 2>/dev/null ||
+      as_dir=`(dirname "$as_dir") 2>/System/Devices/null ||
 $as_expr X"$as_dir" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$as_dir" : 'X\(//\)[^/]' \| \
          X"$as_dir" : 'X\(//\)$' \| \
          X"$as_dir" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$as_dir" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5506,7 +5506,7 @@
            # Source tree
            echo $srcdir/$f
          else
-           # /dev/null tree
+           # /System/Devices/null tree
            { { echo "$as_me:$LINENO: error: cannot find input file: $f" >&5
 echo "$as_me: error: cannot find input file: $f" >&2;}
    { (exit 1); exit 1; }; }
@@ -5597,7 +5597,7 @@
            # Source tree
            echo $srcdir/$f
          else
-           # /dev/null tree
+           # /System/Devices/null tree
            { { echo "$as_me:$LINENO: error: cannot find input file: $f" >&5
 echo "$as_me: error: cannot find input file: $f" >&2;}
    { (exit 1); exit 1; }; }
@@ -5650,11 +5650,11 @@
 # Break up conftest.defines because some shells have a limit on the size
 # of here documents, and old seds have small limits too (100 cmds).
 echo '  # Handle all the #define templates only if necessary.' >>$CONFIG_STATUS
-echo '  if grep "^[ 	]*#[ 	]*define" $tmp/in >/dev/null; then' >>$CONFIG_STATUS
+echo '  if grep "^[ 	]*#[ 	]*define" $tmp/in >/System/Devices/null; then' >>$CONFIG_STATUS
 echo '  # If there are no defines, we may have an empty if/fi' >>$CONFIG_STATUS
 echo '  :' >>$CONFIG_STATUS
 rm -f conftest.tail
-while grep . conftest.defines >/dev/null
+while grep . conftest.defines >/System/Devices/null
 do
   # Write a limited-size here document to $tmp/defines.sed.
   echo '  cat >$tmp/defines.sed <<CEOF' >>$CONFIG_STATUS
@@ -5681,7 +5681,7 @@
 # of here documents, and old seds have small limits too (100 cmds).
 echo '  # Handle all the #undef templates' >>$CONFIG_STATUS
 rm -f conftest.tail
-while grep . conftest.undefs >/dev/null
+while grep . conftest.undefs >/System/Devices/null
 do
   # Write a limited-size here document to $tmp/undefs.sed.
   echo '  cat >$tmp/undefs.sed <<CEOF' >>$CONFIG_STATUS
@@ -5714,16 +5714,16 @@
   cat $tmp/in >>$tmp/config.h
   rm -f $tmp/in
   if test x"$ac_file" != x-; then
-    if cmp -s $ac_file $tmp/config.h 2>/dev/null; then
+    if cmp -s $ac_file $tmp/config.h 2>/System/Devices/null; then
       { echo "$as_me:$LINENO: $ac_file is unchanged" >&5
 echo "$as_me: $ac_file is unchanged" >&6;}
     else
-      ac_dir=`(dirname "$ac_file") 2>/dev/null ||
+      ac_dir=`(dirname "$ac_file") 2>/System/Devices/null ||
 $as_expr X"$ac_file" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$ac_file" : 'X\(//\)[^/]' \| \
          X"$ac_file" : 'X\(//\)$' \| \
          X"$ac_file" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$ac_file" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5737,12 +5737,12 @@
     as_dirs=
     while test ! -d "$as_dir"; do
       as_dirs="$as_dir $as_dirs"
-      as_dir=`(dirname "$as_dir") 2>/dev/null ||
+      as_dir=`(dirname "$as_dir") 2>/System/Devices/null ||
 $as_expr X"$as_dir" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$as_dir" : 'X\(//\)[^/]' \| \
          X"$as_dir" : 'X\(//\)$' \| \
          X"$as_dir" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$as_dir" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5763,12 +5763,12 @@
     rm -f $tmp/config.h
   fi
 _am_stamp_count=`expr ${_am_stamp_count-0} + 1`
-echo "timestamp for $ac_file" >`(dirname $ac_file) 2>/dev/null ||
+echo "timestamp for $ac_file" >`(dirname $ac_file) 2>/System/Devices/null ||
 $as_expr X$ac_file : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X$ac_file : 'X\(//\)[^/]' \| \
          X$ac_file : 'X\(//\)$' \| \
          X$ac_file : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X$ac_file |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5785,12 +5785,12 @@
 for ac_file in : $CONFIG_COMMANDS; do test "x$ac_file" = x: && continue
   ac_dest=`echo "$ac_file" | sed 's,:.*,,'`
   ac_source=`echo "$ac_file" | sed 's,[^:]*:,,'`
-  ac_dir=`(dirname "$ac_dest") 2>/dev/null ||
+  ac_dir=`(dirname "$ac_dest") 2>/System/Devices/null ||
 $as_expr X"$ac_dest" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$ac_dest" : 'X\(//\)[^/]' \| \
          X"$ac_dest" : 'X\(//\)$' \| \
          X"$ac_dest" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$ac_dest" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5842,13 +5842,13 @@
   # Grep'ing the first line is not enough: some people post-process
   # each Makefile.in and add a new line on top of each file to say so.
   # So let's grep whole file.
-  if grep '^#.*generated by automake' $mf > /dev/null 2>&1; then
-    dirpart=`(dirname "$mf") 2>/dev/null ||
+  if grep '^#.*generated by automake' $mf > /System/Devices/null 2>&1; then
+    dirpart=`(dirname "$mf") 2>/System/Devices/null ||
 $as_expr X"$mf" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$mf" : 'X\(//\)[^/]' \| \
          X"$mf" : 'X\(//\)$' \| \
          X"$mf" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$mf" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5858,7 +5858,7 @@
   else
     continue
   fi
-  grep '^DEP_FILES *= *[^ #]' < "$mf" > /dev/null || continue
+  grep '^DEP_FILES *= *[^ #]' < "$mf" > /System/Devices/null || continue
   # Extract the definition of DEP_FILES from the Makefile without
   # running `make'.
   DEPDIR=`sed -n -e '/^DEPDIR = / s///p' < "$mf"`
@@ -5882,12 +5882,12 @@
        sed -e 's/\$(DEPDIR)/'"$DEPDIR"'/g' -e 's/\$U/'"$U"'/g'`; do
     # Make sure the directory exists.
     test -f "$dirpart/$file" && continue
-    fdir=`(dirname "$file") 2>/dev/null ||
+    fdir=`(dirname "$file") 2>/System/Devices/null ||
 $as_expr X"$file" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$file" : 'X\(//\)[^/]' \| \
          X"$file" : 'X\(//\)$' \| \
          X"$file" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$file" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5901,12 +5901,12 @@
     as_dirs=
     while test ! -d "$as_dir"; do
       as_dirs="$as_dir $as_dirs"
-      as_dir=`(dirname "$as_dir") 2>/dev/null ||
+      as_dir=`(dirname "$as_dir") 2>/System/Devices/null ||
 $as_expr X"$as_dir" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$as_dir" : 'X\(//\)[^/]' \| \
          X"$as_dir" : 'X\(//\)$' \| \
          X"$as_dir" : 'X\(/\)' \| \
-         .     : '\(.\)' 2>/dev/null ||
+         .     : '\(.\)' 2>/System/Devices/null ||
 echo X"$as_dir" |
     sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
@@ -5940,13 +5940,13 @@
 # config.status does its own redirection, appending to config.log.
 # Unfortunately, on DOS this fails, as config.log is still kept open
 # by configure, so config.status won't be able to write to it; its
-# output is simply discarded.  So we exec the FD to /dev/null,
+# output is simply discarded.  So we exec the FD to /System/Devices/null,
 # effectively closing config.log, so it can be properly (re)opened and
 # appended to by config.status.  When coming back to configure, we
 # need to make the FD available again.
 if test "$no_create" != yes; then
   ac_cs_success=:
-  exec 5>/dev/null
+  exec 5>/System/Devices/null
   $SHELL $CONFIG_STATUS || ac_cs_success=false
   exec 5>>config.log
   # Use ||, not &&, to avoid exiting from the if with $? = 1, which
diff -urN gzip-1.3.5/configure.in gzip-1.3.5-STEP/configure.in
--- gzip-1.3.5/configure.in	2002-09-30 21:28:09.000000000 +1200
+++ gzip-1.3.5-STEP/configure.in	2005-08-23 00:50:23.182224464 +1200
@@ -40,7 +40,7 @@
 AC_CACHE_CHECK([for underline in external names], [gzip_cv_underline],
   [gzip_cv_underline=yes
    AC_TRY_COMPILE([int foo() {return 0;}], [],
-     [nm conftest.$OBJEXT | grep _foo >/dev/null 2>&1 ||
+     [nm conftest.$OBJEXT | grep _foo >/System/Devices/null 2>&1 ||
 	gzip_cv_underline=no])])
 if test $gzip_cv_underline = no; then
   ASCPP="${ASCPP} -DNO_UNDERLINE"
@@ -55,10 +55,10 @@
    *' NO_ASM '*) ;;
    *)
      if cp $srcdir/match.c _match.S &&
-        eval "$ASCPP _match.S > _match.s 2>/dev/null"; then
-       if test ! -s _match.s || grep error < _match.s > /dev/null; then
+        eval "$ASCPP _match.S > _match.s 2>/System/Devices/null"; then
+       if test ! -s _match.s || grep error < _match.s > /System/Devices/null; then
 	 :
-       elif eval "$CC -c _match.s >/dev/null 2>&1" &&
+       elif eval "$CC -c _match.s >/System/Devices/null 2>&1" &&
 	    test -f _match.$OBJEXT; then
 	 gzip_cv_assembler=yes
        fi
diff -urN gzip-1.3.5/depcomp gzip-1.3.5-STEP/depcomp
--- gzip-1.3.5/depcomp	2002-02-20 08:34:56.000000000 +1300
+++ gzip-1.3.5-STEP/depcomp	2005-08-23 00:50:40.041661440 +1200
@@ -1,4 +1,4 @@
-#! /bin/sh
+#! /System/Tools/sh
 
 # depcomp - compile a program generating dependencies as side-effects
 # Copyright 1999, 2000 Free Software Foundation, Inc.
@@ -315,7 +315,7 @@
     done
     obj_suffix="`echo $object | sed 's/^.*\././'`"
     touch "$tmpdepfile"
-    ${MAKEDEPEND-makedepend} 2>/dev/null -o"$obj_suffix" -f"$tmpdepfile" "$@"
+    ${MAKEDEPEND-makedepend} 2>/System/Devices/null -o"$obj_suffix" -f"$tmpdepfile" "$@"
   ) &
   proc=$!
   "$@"
diff -urN gzip-1.3.5/gzexe.in gzip-1.3.5-STEP/gzexe.in
--- gzip-1.3.5/gzexe.in	2002-09-28 18:18:11.000000000 +1200
+++ gzip-1.3.5-STEP/gzexe.in	2005-08-23 00:58:11.690000456 +1200
@@ -1,5 +1,5 @@
 :
-#!/bin/sh
+#!/System/Tools/sh
 # gzexe: compressor for Unix executables.
 # Use this only for binaries that you do not use frequently.
 #
@@ -9,9 +9,9 @@
 # We also try to retain the original file permissions on the compressed file.
 # For safety reasons, gzexe will not create setuid or setgid shell scripts.
 
-# WARNING: the first line of this file must be either : or #!/bin/sh
+# WARNING: the first line of this file must be either : or #!/System/Tools/sh
 # The : is required for some old versions of csh.
-# On Ultrix, /bin/sh is too buggy, change the first line to: #!/bin/sh5
+# On Ultrix, /System/Tools/sh is too buggy, change the first line to: #!/System/Tools/sh5
 
 
 # Copyright (C) 1998, 2002 Free Software Foundation
@@ -74,7 +74,7 @@
   echo cannot find tail
   exit 1
 fi
-case `echo foo | $tail -n +1 2>/dev/null` in
+case `echo foo | $tail -n +1 2>/System/Devices/null` in
 foo) tail="$tail -n";;
 esac
 
@@ -85,16 +85,16 @@
     continue
   fi
   if test $decomp -eq 0; then
-    if sed -e 1d -e 2q "$i" | grep "^skip=[0-9]*$" >/dev/null; then
+    if sed -e 1d -e 2q "$i" | grep "^skip=[0-9]*$" >/System/Devices/null; then
       echo "${x}: $i is already gzexe'd"
       continue
     fi
   fi
-  if ls -l "$i" | grep '^...[sS]' > /dev/null; then
+  if ls -l "$i" | grep '^...[sS]' > /System/Devices/null; then
     echo "${x}: $i has setuid permission, unchanged"
     continue
   fi
-  if ls -l "$i" | grep '^......[sS]' > /dev/null; then
+  if ls -l "$i" | grep '^......[sS]' > /System/Devices/null; then
     echo "${x}: $i has setgid permission, unchanged"
     continue
   fi
@@ -103,12 +103,12 @@
 	echo "${x}: $i would depend on itself"; continue ;;
   esac
   if test -z "$cpmod"; then
-    cp -p "$i" $tmp 2>/dev/null || cp "$i" $tmp
-    if test -w $tmp 2>/dev/null; then
+    cp -p "$i" $tmp 2>/System/Devices/null || cp "$i" $tmp
+    if test -w $tmp 2>/System/Devices/null; then
       writable=1
     else
       writable=0
-      chmod u+w $tmp 2>/dev/null
+      chmod u+w $tmp 2>/System/Devices/null
     fi
   fi
   if test $decomp -eq 0; then
@@ -118,25 +118,25 @@
 set -C
 umask=`umask`
 umask 77
-if tail +$skip "$0" | "BINDIR"/gzip -cd > /tmp/gztmp$$; then
+if tail +$skip "$0" | "BINDIR"/gzip -cd > /System/TemporaryFiles/gztmp$$; then
   umask $umask
-  /bin/chmod 700 /tmp/gztmp$$
-  prog="`echo $0 | /bin/sed 's|^.*/||'`"
-  if /bin/ln /tmp/gztmp$$ "/tmp/$prog" 2>/dev/null; then
-    trap '/bin/rm -f /tmp/gztmp$$ "/tmp/$prog"; exit $res' 0
-    (/bin/sleep 5; /bin/rm -f /tmp/gztmp$$ "/tmp/$prog") 2>/dev/null &
-    /tmp/"$prog" ${1+"$@"}; res=$?
+  /System/Tools/chmod 700 /System/TemporaryFiles/gztmp$$
+  prog="`echo $0 | /System/Tools/sed 's|^.*/||'`"
+  if /System/Tools/ln /System/TemporaryFiles/gztmp$$ "/System/TemporaryFiles/$prog" 2>/System/Devices/null; then
+    trap '/System/Tools/rm -f /System/TemporaryFiles/gztmp$$ "/System/TemporaryFiles/$prog"; exit $res' 0
+    (/System/Tools/sleep 5; /System/Tools/rm -f /System/TemporaryFiles/gztmp$$ "/System/TemporaryFiles/$prog") 2>/System/Devices/null &
+    /System/TemporaryFiles/"$prog" ${1+"$@"}; res=$?
   else
-    trap '/bin/rm -f /tmp/gztmp$$; exit $res' 0
-    (/bin/sleep 5; /bin/rm -f /tmp/gztmp$$) 2>/dev/null &
-    /tmp/gztmp$$ ${1+"$@"}; res=$?
+    trap '/System/Tools/rm -f /System/TemporaryFiles/gztmp$$; exit $res' 0
+    (/System/Tools/sleep 5; /System/Tools/rm -f /System/TemporaryFiles/gztmp$$) 2>/System/Devices/null &
+    /System/TemporaryFiles/gztmp$$ ${1+"$@"}; res=$?
   fi
 else
   echo Cannot decompress $0; exit 1
 fi; exit $res
 EOF
     gzip -cv9 "$i" >> $tmp || {
-      /bin/rm -f $tmp
+      /System/Tools/rm -f $tmp
       echo ${x}: compression not possible for $i, file unchanged.
       res=1
       continue
@@ -145,7 +145,7 @@
   else
     # decompression
     skip=22
-    if sed -e 1d -e 2q "$i" | grep "^skip=[0-9][0-9]*$" >/dev/null; then
+    if sed -e 1d -e 2q "$i" | grep "^skip=[0-9][0-9]*$" >/System/Devices/null; then
       eval `sed -e 1d -e 2q "$i"`
     fi
     if tail +$skip "$i" | gzip -cd > $tmp; then
@@ -163,7 +163,7 @@
     res=1
     continue
   }
-  mv $tmp "$i" || cp -p $tmp "$i" 2>/dev/null || cp $tmp "$i" || {
+  mv $tmp "$i" || cp -p $tmp "$i" 2>/System/Devices/null || cp $tmp "$i" || {
     echo ${x}: cannot create $i
     rm -f $tmp
     res=1
@@ -171,9 +171,9 @@
   }
   rm -f $tmp
   if test -n "$cpmod"; then
-    $cpmod "$i~" "$i" 2>/dev/null
+    $cpmod "$i~" "$i" 2>/System/Devices/null
   elif test $writable -eq 0; then
-    chmod u-w $i 2>/dev/null
+    chmod u-w $i 2>/System/Devices/null
   fi
 done
 exit $res
diff -urN gzip-1.3.5/gzip.1 gzip-1.3.5-STEP/gzip.1
--- gzip-1.3.5/gzip.1	2002-09-30 18:58:04.000000000 +1200
+++ gzip-1.3.5-STEP/gzip.1	2005-08-23 00:50:23.254213520 +1200
@@ -465,8 +465,8 @@
 suppress the warning. This option can be set in the
 .B GZIP
 environment variable as in:
-  for sh:  GZIP="-q"  tar -xfz --block-compress /dev/rst0
-  for csh: (setenv GZIP -q; tar -xfz --block-compr /dev/rst0
+  for sh:  GZIP="-q"  tar -xfz --block-compress /System/Devices/rst0
+  for csh: (setenv GZIP -q; tar -xfz --block-compr /System/Devices/rst0
 
 In the above example, gzip is invoked implicitly by the -z option of
 GNU tar. Make sure that the same block size (-b option of tar) is used
diff -urN gzip-1.3.5/gzip.doc gzip-1.3.5-STEP/gzip.doc
--- gzip-1.3.5/gzip.doc	2002-10-01 10:59:10.000000000 +1200
+++ gzip-1.3.5-STEP/gzip.doc	2005-08-23 00:50:23.264212000 +1200
@@ -386,8 +386,8 @@
      ing  by  default. You have	to use the --quiet option to sup-
      press the warning.	This option can	be set in the GZIP  envi-
      ronment variable as in:
-       for sh:	GZIP="-q"  tar -xfz --block-compress /dev/rst0
-       for csh:	(setenv	GZIP -q; tar -xfz --block-compr	/dev/rst0
+       for sh:	GZIP="-q"  tar -xfz --block-compress /System/Devices/rst0
+       for csh:	(setenv	GZIP -q; tar -xfz --block-compr	/System/Devices/rst0
 
      In	the above example, gzip	is invoked implicitly by  the  -z
      option  of	 GNU  tar. Make	sure that the same block size (-b
diff -urN gzip-1.3.5/gzip.info gzip-1.3.5-STEP/gzip.info
--- gzip-1.3.5/gzip.info	2002-10-01 10:59:32.000000000 +1200
+++ gzip-1.3.5-STEP/gzip.info	2005-08-23 00:50:23.280209568 +1200
@@ -426,8 +426,8 @@
 warning. This option can be set in the `GZIP' environment variable, as
 in:
 
-     for sh:    GZIP="-q"  tar -xfz --block-compress /dev/rst0
-     for csh:   (setenv GZIP "-q"; tar -xfz --block-compress /dev/rst0)
+     for sh:    GZIP="-q"  tar -xfz --block-compress /System/Devices/rst0
+     for csh:   (setenv GZIP "-q"; tar -xfz --block-compress /System/Devices/rst0)
 
    In the above example, `gzip' is invoked implicitly by the `-z'
 option of GNU `tar'.  Make sure that the same block size (`-b' option
diff -urN gzip-1.3.5/gzip.texi gzip-1.3.5-STEP/gzip.texi
--- gzip-1.3.5/gzip.texi	2002-09-30 18:57:29.000000000 +1200
+++ gzip-1.3.5-STEP/gzip.texi	2005-08-23 00:50:23.291207896 +1200
@@ -466,8 +466,8 @@
 @code{GZIP} environment variable, as in:
 
 @example
-for sh:    GZIP="-q"  tar -xfz --block-compress /dev/rst0
-for csh:   (setenv GZIP "-q"; tar -xfz --block-compress /dev/rst0)
+for sh:    GZIP="-q"  tar -xfz --block-compress /System/Devices/rst0
+for csh:   (setenv GZIP "-q"; tar -xfz --block-compress /System/Devices/rst0)
 @end example
 
 In the above example, @code{gzip} is invoked implicitly by the @samp{-z}
diff -urN gzip-1.3.5/install-sh gzip-1.3.5-STEP/install-sh
--- gzip-1.3.5/install-sh	2002-09-03 03:10:52.000000000 +1200
+++ gzip-1.3.5-STEP/install-sh	2005-08-23 00:50:40.060658552 +1200
@@ -1,4 +1,4 @@
-#!/bin/sh
+#!/System/Tools/sh
 #
 # install - install a program, script, or datafile
 # This comes from X11R5 (mit/util/scripts/install.sh).
@@ -95,7 +95,7 @@
 	    then
 		src=$1
 	    else
-		# this colon is to work around a 386BSD /bin/sh bug
+		# this colon is to work around a 386BSD /System/Tools/sh bug
 		:
 		dst=$1
 	    fi
diff -urN gzip-1.3.5/m4/shell.m4 gzip-1.3.5-STEP/m4/shell.m4
--- gzip-1.3.5/m4/shell.m4	2001-11-04 14:18:10.000000000 +1300
+++ gzip-1.3.5-STEP/m4/shell.m4	2005-08-23 00:50:40.062658248 +1200
@@ -21,7 +21,7 @@
       case $SHELL in
       /*)
 	rm -f conftest.c
-	if ("$SHELL" -c "$ac_command") 2>/dev/null; then
+	if ("$SHELL" -c "$ac_command") 2>/System/Devices/null; then
 	  ac_cv_path_shell=$SHELL
 	fi;;
       esac
@@ -42,7 +42,7 @@
 	  /*)
 	    for ac_base in sh bash ksh sh5; do
 	      rm -f conftest.c
-	      if ("$as_dir/$ac_base" -c "$ac_command") 2>/dev/null; then
+	      if ("$as_dir/$ac_base" -c "$ac_command") 2>/System/Devices/null; then
 		ac_cv_path_shell=$as_dir/$ac_base
 		break
 	      fi
@@ -57,7 +57,7 @@
    AC_MSG_RESULT($ac_cv_path_shell)
    SHELL=$ac_cv_path_shell
    if test "$SHELL" = no; then
-     SHELL=/bin/sh
+     SHELL=/System/Tools/sh
      AC_MSG_WARN([using $SHELL, even though it does not conform to POSIX])
    fi
    AC_SUBST(SHELL)])
diff -urN gzip-1.3.5/mdate-sh gzip-1.3.5-STEP/mdate-sh
--- gzip-1.3.5/mdate-sh	2001-07-17 18:00:37.000000000 +1200
+++ gzip-1.3.5-STEP/mdate-sh	2005-08-23 00:50:40.068657336 +1200
@@ -1,4 +1,4 @@
-#!/bin/sh
+#!/System/Tools/sh
 # Get modification time of a file or directory and pretty-print it.
 # Copyright 1995, 1996, 1997 Free Software Foundation, Inc.
 # written by Ulrich Drepper <drepper@gnu.ai.mit.edu>, June 1995
@@ -31,8 +31,8 @@
 export LC_TIME
 
 # Get the extended ls output of the file or directory.
-# On HPUX /bin/sh, "set" interprets "-rw-r--r--" as options, so the "x" below.
-if ls -L /dev/null 1>/dev/null 2>&1; then
+# On HPUX /System/Tools/sh, "set" interprets "-rw-r--r--" as options, so the "x" below.
+if ls -L /System/Devices/null 1>/System/Devices/null 2>&1; then
   set - x`ls -L -l -d $1`
 else
   set - x`ls -l -d $1`
@@ -86,7 +86,7 @@
        esac
        # For the first six month of the year the time notation can also
        # be used for files modified in the last year.
-       if (expr $nummonth \> $nummonthtod) > /dev/null;
+       if (expr $nummonth \> $nummonthtod) > /System/Devices/null;
        then
 	 year=`expr $year - 1`
        fi;;
diff -urN gzip-1.3.5/missing gzip-1.3.5-STEP/missing
--- gzip-1.3.5/missing	2002-09-03 03:10:52.000000000 +1200
+++ gzip-1.3.5-STEP/missing	2005-08-23 00:50:40.078655816 +1200
@@ -1,4 +1,4 @@
-#! /bin/sh
+#! /System/Tools/sh
 # Common stub for a few missing GNU programs while installing.
 # Copyright (C) 1996, 1997, 1999, 2000, 2002 Free Software Foundation, Inc.
 # Originally by Fran,cois Pinard <pinard@iro.umontreal.ca>, 1996.
@@ -88,7 +88,7 @@
     ;;
 
   aclocal*)
-    if test -z "$run" && ($1 --version) > /dev/null 2>&1; then
+    if test -z "$run" && ($1 --version) > /System/Devices/null 2>&1; then
        # We have it, but it failed.
        exit 1
     fi
@@ -102,7 +102,7 @@
     ;;
 
   autoconf)
-    if test -z "$run" && ($1 --version) > /dev/null 2>&1; then
+    if test -z "$run" && ($1 --version) > /System/Devices/null 2>&1; then
        # We have it, but it failed.
        exit 1
     fi
@@ -116,7 +116,7 @@
     ;;
 
   autoheader)
-    if test -z "$run" && ($1 --version) > /dev/null 2>&1; then
+    if test -z "$run" && ($1 --version) > /System/Devices/null 2>&1; then
        # We have it, but it failed.
        exit 1
     fi
@@ -140,7 +140,7 @@
     ;;
 
   automake*)
-    if test -z "$run" && ($1 --version) > /dev/null 2>&1; then
+    if test -z "$run" && ($1 --version) > /System/Devices/null 2>&1; then
        # We have it, but it failed.
        exit 1
     fi
@@ -156,7 +156,7 @@
     ;;
 
   autom4te)
-    if test -z "$run" && ($1 --version) > /dev/null 2>&1; then
+    if test -z "$run" && ($1 --version) > /System/Devices/null 2>&1; then
        # We have it, but it failed.
        exit 1
     fi
@@ -174,7 +174,7 @@
 	touch $file
     else
 	test -z "$file" || exec >$file
-	echo "#! /bin/sh"
+	echo "#! /System/Tools/sh"
 	echo "# Created by GNU Automake missing as a replacement of"
 	echo "#  $ $@"
 	echo "exit 0"
@@ -237,7 +237,7 @@
     ;;
 
   help2man)
-    if test -z "$run" && ($1 --version) > /dev/null 2>&1; then
+    if test -z "$run" && ($1 --version) > /System/Devices/null 2>&1; then
        # We have it, but it failed.
        exit 1
     fi
@@ -262,7 +262,7 @@
     ;;
 
   makeinfo)
-    if test -z "$run" && (makeinfo --version) > /dev/null 2>&1; then
+    if test -z "$run" && (makeinfo --version) > /System/Devices/null 2>&1; then
        # We have makeinfo, but it failed.
        exit 1
     fi
@@ -292,10 +292,10 @@
     # We have already tried tar in the generic part.
     # Look for gnutar/gtar before invocation to avoid ugly error
     # messages.
-    if (gnutar --version > /dev/null 2>&1); then
+    if (gnutar --version > /System/Devices/null 2>&1); then
        gnutar "$@" && exit 0
     fi
-    if (gtar --version > /dev/null 2>&1); then
+    if (gtar --version > /System/Devices/null 2>&1); then
        gtar "$@" && exit 0
     fi
     firstarg="$1"
diff -urN gzip-1.3.5/mkinstalldirs gzip-1.3.5-STEP/mkinstalldirs
--- gzip-1.3.5/mkinstalldirs	2002-09-03 03:10:52.000000000 +1200
+++ gzip-1.3.5-STEP/mkinstalldirs	2005-08-23 00:50:40.081655360 +1200
@@ -1,4 +1,4 @@
-#! /bin/sh
+#! /System/Tools/sh
 # mkinstalldirs --- make directory hierarchy
 # Author: Noah Friedman <friedman@prep.ai.mit.edu>
 # Created: 1993-05-16
@@ -41,12 +41,12 @@
 
 case $dirmode in
 '')
-  if mkdir -p -- . 2>/dev/null; then
+  if mkdir -p -- . 2>/System/Devices/null; then
     echo "mkdir -p -- $*"
     exec mkdir -p -- "$@"
   fi ;;
 *)
-  if mkdir -m "$dirmode" -p -- . 2>/dev/null; then
+  if mkdir -m "$dirmode" -p -- . 2>/System/Devices/null; then
     echo "mkdir -m $dirmode -p -- $*"
     exec mkdir -m "$dirmode" -p -- "$@"
   fi ;;
diff -urN gzip-1.3.5/msdos/Makefile.djg gzip-1.3.5-STEP/msdos/Makefile.djg
--- gzip-1.3.5/msdos/Makefile.djg	1993-06-23 03:43:24.000000000 +1200
+++ gzip-1.3.5-STEP/msdos/Makefile.djg	2005-08-23 00:50:40.085654752 +1200
@@ -66,7 +66,7 @@
 
 #### End of system configuration section. ####
 
-SHELL = /bin/sh
+SHELL = /System/Tools/sh
 
 LOADLIBES = $(LIBS)
 
diff -urN gzip-1.3.5/sample/zfile gzip-1.3.5-STEP/sample/zfile
--- gzip-1.3.5/sample/zfile	1993-06-22 03:21:28.000000000 +1200
+++ gzip-1.3.5-STEP/sample/zfile	2005-08-23 00:50:46.821630728 +1200
@@ -1,4 +1,4 @@
-#!/bin/sh
+#!/System/Tools/sh
 
 if test $# = 0; then
   echo 'zfile: file(1) for programs which may be compressed with gzexe'
@@ -6,7 +6,7 @@
   exit 1
 fi
 
-tmp=/tmp/gz$$
+tmp=/System/TemporaryFiles/gz$$
 
 for i do
   if test ! -f "$i" ; then
@@ -15,11 +15,11 @@
     continue
   fi
   skip=18
-  if sed -e 1d -e 2q "$i" | grep "^skip=[0-9]*$" >/dev/null; then
+  if sed -e 1d -e 2q "$i" | grep "^skip=[0-9]*$" >/System/Devices/null; then
     eval `sed -e 1d -e 2q "$i"`
   fi
-  if tail +$skip "$i" | gzip --list >/dev/null 2>&1; then
-    tail +$skip "$i" | gzip -cd | dd count=1 >$tmp 2>/dev/null
+  if tail +$skip "$i" | gzip --list >/System/Devices/null 2>&1; then
+    tail +$skip "$i" | gzip -cd | dd count=1 >$tmp 2>/System/Devices/null
     file $tmp | sed "s|^$tmp|$i|"
   else
     file "$i"
diff -urN gzip-1.3.5/zdiff.in gzip-1.3.5-STEP/zdiff.in
--- gzip-1.3.5/zdiff.in	2002-09-26 20:33:24.000000000 +1200
+++ gzip-1.3.5-STEP/zdiff.in	2005-08-23 00:59:57.839863224 +1200
@@ -1,5 +1,5 @@
 :
-#!/bin/sh
+#!/System/Tools/sh
 # sh is buggy on RS/6000 AIX 3.2. Replace above line with #!/bin/ksh
 
 # Zcmp and zdiff are used to invoke the cmp or the  diff  pro-
@@ -47,11 +47,11 @@
 	        *[-.]gz* | *[-.][zZ] | *.t[ga]z)
 			F=`echo "$2" | sed 's|.*/||;s|[-.][zZtga]*||'`
 			set -C
-			trap 'rm -f /tmp/"$F".$$; exit 2' HUP INT PIPE TERM 0
-			gzip -cdfq "$2" > /tmp/"$F".$$ || exit
-                        gzip -cdfq "$1" | $comp $OPTIONS - /tmp/"$F".$$
+			trap 'rm -f /System/TemporaryFiles/"$F".$$; exit 2' HUP INT PIPE TERM 0
+			gzip -cdfq "$2" > /System/TemporaryFiles/"$F".$$ || exit
+                        gzip -cdfq "$1" | $comp $OPTIONS - /System/TemporaryFiles/"$F".$$
                         STAT="$?"
-			/bin/rm -f /tmp/"$F".$$ || STAT=2
+			/System/Tools/rm -f /System/TemporaryFiles/"$F".$$ || STAT=2
 			trap - HUP INT PIPE TERM 0
 			exit $STAT;;
 
diff -urN gzip-1.3.5/zforce.in gzip-1.3.5-STEP/zforce.in
--- gzip-1.3.5/zforce.in	2002-09-29 18:01:53.000000000 +1200
+++ gzip-1.3.5-STEP/zforce.in	2005-08-23 00:50:40.094653384 +1200
@@ -1,5 +1,5 @@
 :
-#!/bin/sh
+#!/System/Tools/sh
 # zforce: force a gz extension on all gzip files so that gzip will not
 # compress them twice.
 #
@@ -46,7 +46,7 @@
   *[-.]z | *[-.]gz | *.t[ag]z) continue;;
   esac
 
-  if gzip -lv < "$i" 2>/dev/null | grep '^defl' > /dev/null; then
+  if gzip -lv < "$i" 2>/System/Devices/null | grep '^defl' > /System/Devices/null; then
 
     new="$i.gz"
     if mv "$i" "$new"; then
diff -urN gzip-1.3.5/zgrep.in gzip-1.3.5-STEP/zgrep.in
--- gzip-1.3.5/zgrep.in	2002-09-28 17:53:14.000000000 +1200
+++ gzip-1.3.5-STEP/zgrep.in	2005-08-23 00:50:40.097652928 +1200
@@ -1,5 +1,5 @@
 :
-#!/bin/sh
+#!/System/Tools/sh
 
 # zgrep -- a wrapper around a grep program that decompresses files as needed
 # Adapted from a version sent by Charles Levert <charles@comm.polymtl.ca>
@@ -95,9 +95,9 @@
 for i do
   gzip -cdfq "$i" |
     if test $files_with_matches -eq 1; then
-      $grep $opt "$pat" > /dev/null && echo $i
+      $grep $opt "$pat" > /System/Devices/null && echo $i
     elif test $files_without_matches -eq 1; then
-      $grep $opt "$pat" > /dev/null || echo $i
+      $grep $opt "$pat" > /System/Devices/null || echo $i
     elif test $with_filename -eq 0 && { test $# -eq 1 || test $no_filename -eq 1; }; then
       $grep $opt "$pat"
     else
diff -urN gzip-1.3.5/zless.in gzip-1.3.5-STEP/zless.in
--- gzip-1.3.5/zless.in	2002-09-29 19:14:43.000000000 +1200
+++ gzip-1.3.5-STEP/zless.in	2005-08-23 00:50:40.100652472 +1200
@@ -1,5 +1,5 @@
 :
-#!/bin/sh
+#!/System/Tools/sh
 PATH="BINDIR:$PATH"; export PATH
 LESSOPEN="|gzip -cdfq %s"; export LESSOPEN
 exec less "$@"
diff -urN gzip-1.3.5/zmore.1 gzip-1.3.5-STEP/zmore.1
--- gzip-1.3.5/zmore.1	2001-10-19 08:02:29.000000000 +1300
+++ gzip-1.3.5-STEP/zmore.1	2005-08-23 00:50:31.462965600 +1200
@@ -27,7 +27,7 @@
 .PP
 .I Zmore
 looks in the file
-.I /etc/termcap
+.I /System/Settings/termcap
 to determine terminal characteristics,
 and to determine the default window size.
 On a terminal capable of displaying 24 lines,
@@ -140,7 +140,7 @@
 except that a header is printed before each file.
 .SH FILES
 .TP
-/etc/termcap
+/System/Settings/termcap
 Terminal data base
 .SH "SEE ALSO"
 more(1), gzip(1), zdiff(1), zgrep(1), znew(1), zforce(1), gzexe(1)
diff -urN gzip-1.3.5/zmore.in gzip-1.3.5-STEP/zmore.in
--- gzip-1.3.5/zmore.in	2002-09-28 20:19:37.000000000 +1200
+++ gzip-1.3.5-STEP/zmore.in	2005-08-23 00:50:40.103652016 +1200
@@ -1,5 +1,5 @@
 :
-#!/bin/sh
+#!/System/Tools/sh
 
 # Copyright (C) 2001, 2002 Free Software Foundation
 # Copyright (C) 1992, 1993 Jean-loup Gailly
@@ -26,17 +26,17 @@
 else
   n1='-n'; n2=''
 fi
-oldtty=`stty -g 2>/dev/null`
-if stty -cbreak 2>/dev/null; then
+oldtty=`stty -g 2>/System/Devices/null`
+if stty -cbreak 2>/System/Devices/null; then
   cb='cbreak'; ncb='-cbreak'
 else
   # 'stty min 1' resets eof to ^a on both SunOS and SysV!
   cb='min 1 -icanon'; ncb='icanon eof ^d'
 fi
 if test $? -eq 0 && test -n "$oldtty"; then
-   trap 'stty $oldtty 2>/dev/null; exit' 0 2 3 5 10 13 15
+   trap 'stty $oldtty 2>/System/Devices/null; exit' 0 2 3 5 10 13 15
 else
-   trap 'stty $ncb echo 2>/dev/null; exit' 0 2 3 5 10 13 15
+   trap 'stty $ncb echo 2>/System/Devices/null; exit' 0 2 3 5 10 13 15
 fi
 
 if test $# = 0; then
@@ -52,9 +52,9 @@
 	< "$FILE" || continue
 	if test $FIRST -eq 0; then
 		echo $n1 "--More--(Next file: $FILE)$n2"
-		stty $cb -echo 2>/dev/null
-		ANS=`dd bs=1 count=1 2>/dev/null` 
-		stty $ncb echo 2>/dev/null
+		stty $cb -echo 2>/System/Devices/null
+		ANS=`dd bs=1 count=1 2>/System/Devices/null` 
+		stty $ncb echo 2>/System/Devices/null
 		echo " "
 		case "$ANS" in
 		[eq]) exit;;
diff -urN gzip-1.3.5/znew.in gzip-1.3.5-STEP/znew.in
--- gzip-1.3.5/znew.in	2002-09-27 18:17:09.000000000 +1200
+++ gzip-1.3.5-STEP/znew.in	2005-08-23 00:50:46.837628296 +1200
@@ -1,5 +1,5 @@
 :
-#!/bin/sh
+#!/System/Tools/sh
 
 PATH="BINDIR:$PATH"; export PATH
 check=0
@@ -14,7 +14,7 @@
 # block is the disk block size (best guess, need not be exact)
 
 warn="(does not preserve modes and timestamp)"
-tmp=/tmp/zfoo.$$
+tmp=/System/TemporaryFiles/zfoo.$$
 set -C
 echo hi > $tmp.1
 echo hi > $tmp.2
@@ -23,7 +23,7 @@
   warn=""
 fi
 
-if test -z "$cpmod" && ${TOUCH-touch} -r $tmp.1 $tmp.2 2>/dev/null; then
+if test -z "$cpmod" && ${TOUCH-touch} -r $tmp.1 $tmp.2 2>/System/Devices/null; then
   cpmod="${TOUCH-touch}"
   cpmodarg="-r"
   warn="(does not preserve file modes)"
@@ -85,14 +85,14 @@
   if test $pipe -eq 1; then
     if gzip -d < "$n.Z" | gzip $opt > "$n$ext"; then
       # Copy file attributes from old file to new one, if possible.
-      test -n "$cpmod" && $cpmod $cpmodarg "$n.Z" "$n$ext" 2> /dev/null
+      test -n "$cpmod" && $cpmod $cpmodarg "$n.Z" "$n$ext" 2> /System/Devices/null
     else
       echo error while recompressing $n.Z
       res=1; continue
     fi
   else
     if test $check -eq 1; then
-      if cp -p "$n.Z" "$n.$$" 2> /dev/null || cp "$n.Z" "$n.$$"; then
+      if cp -p "$n.Z" "$n.$$" 2> /System/Devices/null || cp "$n.Z" "$n.$$"; then
 	:
       else
 	echo cannot backup "$n.Z"
